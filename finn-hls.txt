文件结构：



[mac.hpp]
      ->[utils.hpp]
      ->mul (DSP/LUT)
      ->mac (MMV) output datetype=accumulation type
      
[mvau.hpp]
      ->[mac.hpp]
      ->in: stream
            ->NF:在H上，处理几次可以获得所有输出
            ->inputBuf: SF=MatrixW/SIMD(input_col_parallel)(resolving a memory into individual registers)(一行分几批处理)
            ->input matrix: MatrixW,MatrixH
      ->out
            ->accu: (out_parallel_W),(out_parallel_H)(行/列并行输出)(resolving a memory into individual registers)
      ->weight: weight matrix
      
      ->reps: number of image
      ->Total_Fold:  (MatrixW/SIMD)(MatrixH/PE)
      ->计算循环：总循环次数Total_Fold*图片数量
            ->每次循环：
                  ->在inputBuf[sf]中存入in stream的一个数据，数据存在inElem中
                  ->sf=0时，初始化accu
                  ->对PE(输出行并行数)进行循环
                        ->
          


注记：
1、#pragma HLS inline:
HLS会自动的INLINE小的函数，当函数被INLINE的时候，就不会有独立的report和RTL file。所以，INLINE理解为，子函数硬件结构直接融入到上层函数之中，INLINE OFF理解为子函数硬件结构独立存在，用上层函数来调用此子函数。保险起见，我们需要手动加入相应的优化指令，确定为INLINE或者INLINE off
2、LUT单元:  
LookUp Table

